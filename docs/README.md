# Documentation Index

- `architecture/whatsapp-agent-architecture.md` - how the WhatsApp
  Operations Agent works end to end (request flow, LangGraph nodes, order
  state machine).
- `architecture/privacy-firewall.md` - what PII masking exists, where it's
  enforced, and its known gaps.
- `decisions/current-build-decisions.md` - the decisions in force for this
  build (mock-first, no classifier yet, admin approval required, etc).
- `checklists/live-whatsapp-readiness.md` - everything required before any
  live WhatsApp/Meta integration is turned on.
- `checklists/admin-dashboard-mvp.md` - what the backend exposes today for
  a future admin dashboard frontend, and what's still missing.
- `audits/fresh-project-start-report.md` - the review of the existing
  `LaundryKhalasPrototype` project performed before starting this build,
  and what was/wasn't reused from it.

See the top-level `README.md` for how to actually run the project.
